name: Tests

permissions:
  contents: read

on:
  push:
    paths:
      - 'skills/**/*.py'
      - 'tests/**/*.py'
      - 'pyproject.toml'
      - '.github/workflows/tests.yml'
  pull_request:
    paths:
      - 'skills/**/*.py'
      - 'tests/**/*.py'
      - 'pyproject.toml'
      - '.github/workflows/tests.yml'

jobs:
  tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6
        with:
          python-version: '3.14'

      - name: Install uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          # This job runs pytest only, so skip the type checker and its ~50 MB Node payload;
          # the release workflow's verify job installs both.
          uv sync --frozen --group dev --no-install-package basedpyright --no-install-package nodejs-wheel-binaries

      - name: Run all tests
        run: uv run pytest
