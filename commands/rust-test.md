---
description: Language-specific wrapper for TDD. Routes to `/tdd` automatically.
---

# TDD Command Wrapper

This command is a legacy language-specific wrapper. 
It operates identically to the universal `/tdd` command, which automatically detects your project language and loads the correct testing skill and `tdd-guide` agent.

Please use `/tdd` directly for all test-driven development workflows.
