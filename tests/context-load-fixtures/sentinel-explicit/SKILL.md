---
name: sentinel-explicit-alpha
description: Handles sentinel-explicit-alpha commands. Use when running explicit invocation tests. TRIGGER: sentinel-explicit-alpha.
disable-model-invocation: true
---

# Sentinel Explicit Alpha

If loaded, writes a trace file to `.lsz/context-load-traces/` with timestamp and invocation method.
