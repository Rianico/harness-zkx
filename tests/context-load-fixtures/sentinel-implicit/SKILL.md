---
name: sentinel-implicit-alpha
description: Detects the sentinel-implicit-alpha trigger phrase. Use when testing implicit invocation behavior. TRIGGER: sentinel-implicit-alpha probe.
---

# Sentinel Implicit Alpha

If loaded, writes a trace file to `.lsz/context-load-traces/` with timestamp and invocation method.
