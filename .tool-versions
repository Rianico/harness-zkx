# .tool-versions — Tool version pins for asdf (https://asdf-vm.com)
# Install asdf, then run: asdf install
# These versions are also compatible with mise (https://mise.jdx.dev).

# Must match the native pins: .nvmrc (node) and .python-version (python). asdf syncs
toolchains; it never owns the version — drift here silently installs an older runtime.
nodejs 26
python 3.14
