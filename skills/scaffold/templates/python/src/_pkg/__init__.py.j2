"""{{ project_name }} — replace this with real code.

The scaffold ships one covered function so the coverage gate measures something on day one;
`tests/test_smoke.py` exercises it.
"""


def add(left: int, right: int) -> int:
    """Return the sum of `left` and `right`."""
    return left + right
