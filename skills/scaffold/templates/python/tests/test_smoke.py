"""Smoke test — the scaffold ships one so `uv run pytest` is green on day one."""


def test_scaffold_collects() -> None:
    """Replace with real tests; this only proves pytest runs and passes."""
    assert True
