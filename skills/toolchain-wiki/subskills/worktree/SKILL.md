---
name: worktree
description: >-
  Git worktree lifecycle via Worktrunk (branch-addressed, hooks, hash_port, LLM commits). Use when managing parallel worktrees, configuring worktrunk, or debugging shell integration. TRIGGER: worktrunk, wt, git worktree, worktree, hash_port
metadata:
  managed-by: toolchain-wiki
---

# Worktree — Worktrunk

Proxy to canonical `worktrunk-guide` (global Pi skill + `branch-worktree-pr` workflow).

- Canonical: `worktrunk-guide` (`~/.pi/agent/skills/worktrunk-guide/SKILL.md` in Pi, or `skills/branch-worktree-pr/` in repo)
- Config: `references/config.md` under canonical

Use the canonical skill for all work. This proxy exists only for `toolchain-wiki` dispatch:

- `toolchain-wiki` → `worktree` → Read canonical
- Direct: `$skill worktrunk-guide` or `Read worktrunk-guide` also works

Do not duplicate content here — single source is `worktrunk-guide`.
